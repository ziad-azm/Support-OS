# UX Issue Register

Produced by `DSN-6` (Story 61, `SupportOs backlog.MD:558`) — one systematic
walk of every already-built route (`frontend/src/app/router.tsx`) against a
Nielsen-heuristic checklist (consistency, feedback, error prevention,
recognition-over-recall, minimalism, match to real workflow) plus a
responsive and a bilingual pass. This is the "identify" half of the
`DSN-6`–`DSN-13` thread — `DSN-7`–`DSN-12` fix by category below, `DSN-13`
verifies. See `CONVENTIONS.md` § 25 for prior `DSN` decisions this register
does not restate.

**ID:** `UX-###`, sequential, assigned once in walk order — never renumbered
or reused, even if a finding is later closed as a duplicate.

**Category → owning story** (fixed by `SupportOs backlog.MD:566-630`, not a
per-finding judgment call beyond picking the right category):

| Category | Owning story |
|---|---|
| `consistency` | `DSN-7` |
| `interaction` | `DSN-8` |
| `responsive` | `DSN-9` |
| `form` | `DSN-10` |
| `IA` | `DSN-11` |
| `content` | `DSN-12` |
| `bilingual` | `DSN-12` |

**Severity:** `critical` (blocks or corrupts a core workflow, or an
accessibility floor `DSN-2` didn't already cover), `major` (clearly wrong or
inconsistent, workable around), `minor` (polish).

**Status:** `Open` (not yet actioned), `Resolved (Story NN)` (fixed, or a
verified false positive requiring no change), `Resolved (Story NN) —
partial` (fixed for part of the finding's scope; the remainder stays `Open`
or is separately noted), `Deferred` (a real gap, but its fix is outside the
`DSN-6`–`DSN-13` frontend-only guardrail — needs a dedicated non-`DSN` story
or an explicit exception).

**Totals: 66 findings** — 5 `critical`, 28 `major`, 33 `minor`. By owning
story: `DSN-7` 13, `DSN-8` 18, `DSN-9` 5, `DSN-10` 6, `DSN-11` 12, `DSN-12` 12.

**Story 62 (`DSN-7`), consistency findings:** 9 resolved, 2 partially
resolved, 1 deferred, of the 12 findings `DSN-6` originally catalogued as
`consistency` — plus one new finding (`UX-066`) discovered and fixed during
`DSN-7`'s own implementation, not present in `DSN-6`'s original walk. Two of
the register's own recommended fixes were corrected after verification
during planning (`UX-036`, `UX-044`) and one was found to be a verified
false positive requiring no code change (`UX-047`) — see each row's Finding
column for the corrected reasoning.

| ID | Screen | Category | Severity | Finding | Recommended fix | Owning story | Status |
|---|---|---|---|---|---|---|---|
| UX-001 | Shell — `Sidebar.tsx` (staff) and `PortalLayout.tsx` (portal) | IA | major | Neither nav uses `NavLink`/`aria-current` or any current-path check — `SidebarLink` (`Sidebar.tsx:46-74`) and every `<Link>` in `PortalLayout.tsx:30-47` render identically regardless of the active route. A user has no visual confirmation of where they are in either shell. | Swap `Link` for `NavLink` (or add a `useLocation` pathname check) in both `SidebarLink` and `PortalLayout`'s nav buttons; apply a distinct active style and `aria-current="page"`. | DSN-11 | Open |
| UX-002 | Shell — `Sidebar.tsx` nav | IA | minor | The nav (`Sidebar.tsx:122-243`) is one flat, unlabeled list — the Knowledge Base (3 links, lines 159-178) and Reports (5 links, lines 195-226) sub-groups have no section heading separating them from single-link items like Customers or Tasks. | Add a labeled section wrapper/heading around each multi-link group. | DSN-11 | Open |
| UX-003 | Shell — `Sidebar.tsx` Reports links | consistency | minor | All 5 Reports sidebar links (`Sidebar.tsx:195-226`) share the identical `ChartNoAxesColumnIcon` — no per-report differentiation. The first link's label key also breaks its own siblings' naming convention: `t('reports:title')` (line 199) vs. `sidebarSla`/`sidebarAgents`/`sidebarCsat`/`sidebarDashboard` (lines 205/211/217/223). | Give each report link a distinct icon; rename the `reports:title`-keyed link's key to `sidebarTickets` to match its siblings. | DSN-7 | Resolved (Story 62) |
| UX-004 | `/login` — `features/auth/components/LoginPage.tsx` | interaction | minor | No "forgot password"/recovery link anywhere on the page — the only failure path is a generic wrong-credentials toast. A locked-out staff user has no in-app next step. | Add a short line under the form pointing to an admin/support contact if self-service reset is out of scope. | DSN-8 | Open |
| UX-005 | `/chat` — `features/live-chat/components/LiveChatWidget.tsx` | interaction | critical | `StartForm.onSubmit` (`LiveChatWidget.tsx:49-59`) calls `startLiveChat(...)` directly (not via `useMutation`) wrapped only in `try/finally` — a failed submit (network/5xx/validation) is swallowed silently; the button just re-enables with no error shown. `LoginPage.tsx:35-46` uses `useMutation` for exactly this reason. | Convert to `useMutation` with `onError` surfaced via the existing toast/`FormErrorSummary` machinery, matching `LoginPage`. | DSN-8 | Open |
| UX-006 | `/chat` — `features/live-chat/components/LiveChatWidget.tsx` | interaction | major | `ChatPane`'s `WebSocket` (lines 106-116) sets only `onmessage` — no `onerror`/`onclose`, no reconnect/backoff, no disconnected-state UI. `onSubmit` (lines 122-125) calls `socketRef.current?.send(...)` and clears the composer even if the socket has dropped, silently no-op-ing the send. | Track connection state, show a disconnected/reconnecting indicator, and don't clear the composer unless the socket is actually `OPEN`. | DSN-8 | Open |
| UX-007 | `/chat` — `features/live-chat/components/LiveChatWidget.tsx` | interaction | major | A resumed session (`localStorage`-persisted `{ticketId, sessionToken}`, lines 36-41) always initializes `messages` to `[]` (line 101) with no REST fetch of prior transcript — a customer who refreshes mid-conversation sees "No messages yet" even though history exists server-side. | On mount with an existing session, fetch prior message history over REST before/alongside opening the socket. | DSN-8 | Open |
| UX-008 | `/chat` — `features/live-chat/components/LiveChatWidget.tsx` | responsive | major | `ChatPane`'s `Card` uses a hardcoded `style={{ height: '32rem' }}` (line 128) inside `PublicLayout`'s non-scrolling centered container (`PublicLayout.tsx:13`). On a short viewport (~550px height), the fixed 512px card overflows top/bottom with no scroll fallback. | Replace with a viewport-relative cap, e.g. `h-[min(32rem,calc(100dvh-3rem))]`, or add `overflow-y-auto` to `PublicLayout`'s container. | DSN-9 | Open |
| UX-009 | `/contact` — `features/web-form/components/WebFormPage.tsx` | interaction | critical | Same bug as `LiveChatWidget`: `WebForm.onSubmit` (lines 75-89) calls `submitWebForm(...)` directly with only `try/finally`, no `catch`/mutation wiring — a failed anonymous ticket submission is silent. This is the sole way an anonymous customer files a request. | Convert to `useMutation` with `onError` surfacing to `FormErrorSummary`/toast. | DSN-8 | Open |
| UX-010 | `/contact` — `features/web-form/components/WebFormPage.tsx` | form | minor | `useWebFormCategories()` (line 63) has no loading/error affordance passed to `SelectField` — while loading, the select silently shows only "No category"; on error there's no inline message at all. | Pass `disabled={categoriesQuery.isLoading}` to `SelectField` and render an inline error state on `categoriesQuery.isError`. | DSN-10 | Open |
| UX-011 | `/contact` — `features/web-form/components/WebFormPage.tsx` | form | minor | `description` is validated to `requiredString(5000)` (`schemas.ts:11-13`), but `TextareaField` never sets `maxLength` and no character counter is shown — the 5000-char cap is invisible until submit fails. | Add an optional `maxLength`/counter prop to the shared `TextareaField`. | DSN-10 | Open |
| UX-012 | `/login`, `/chat`, `/contact` — `features/auth/locales/ar.json`, `features/live-chat/locales/ar.json`, `features/web-form/locales/ar.json` | bilingual | major | These three public-facing files use colloquial/dialectal Arabic (e.g. `auth/ar.json:9` "مش من فريق العمل؟", `live-chat/ar.json:4/13/15`), while `tickets/ar.json` and the rest of the app use consistent formal MSA — a register mismatch on exactly the screens facing external/anonymous customers. | Rewrite the customer-facing strings in these 3 files in formal MSA to match the rest of the app. | DSN-12 | Open |
| UX-013 | `/` (index) — `features/health/components/HealthPage.tsx` | consistency | major | The landing route every authenticated user hits after login renders a bare, unstyled `<div>`/`<ul>`/native `<button>` (`HealthPage.tsx:17-34`) — no `PageHeader`, no `Card`, no shared `Button` — the sole screen in the app with zero shared-primitive usage, despite being the first thing seen post-login. | Wrap in `PageHeader`/`Card` and swap the native `<button>` for the shared `Button`, matching every other screen's structure. | DSN-7 | Resolved (Story 62) |
| UX-014 | `/customers/:id` — `features/customers/components/CustomerProfilePage.tsx` | responsive | minor | The summary `<dl className="grid grid-cols-2 gap-4">` (line 61) has no responsive fallback — stays two columns at 375px width, cramping longer Arabic labels/values. | Change to `grid-cols-1 sm:grid-cols-2`. | DSN-9 | Open |
| UX-015 | `/customers/:id` — `features/customers/components/AttachmentsSection.tsx` | bilingual | minor | `formatSize` (lines 35-39) hardcodes unit suffixes `'B'`/`'KB'`/`'MB'` in template literals, not via `t(...)` — file sizes always render in English abbreviations regardless of UI language. | Move unit labels into the `customers` locale namespace and interpolate via `t(...)`. | DSN-12 | Open |
| UX-016 | `/customers`, `/tickets`, `/tickets/my-tickets` — `CustomerListPage.tsx`, `TicketListPage.tsx`, `MyTicketsPage.tsx` | interaction | minor | None of the three list pages has a row-selection checkbox column or bulk-action bar — every delete/edit is single-row only. (Explicitly still-open per `DSN-2`'s deferred list.) | Add an opt-in checkbox column + contextual bulk-action bar to the shared `DataTable`, starting with `TicketListPage`/`MyTicketsPage`. | DSN-8 | Open |
| UX-017 | `/tickets/:id` — `features/tickets/components/TicketStatusControl.tsx` | interaction | major | `onValueChange` (lines 38-45) fires the status-change mutation immediately on selection, with only a toast, no confirmation — unlike `TicketDetailPage`'s delete/escalation-toggle, which both use `useConfirm()`. `closed` has zero outgoing transitions (`ticket.ts:19`), so this is a one-click irreversible action. | Route any transition to a terminal status (zero outgoing transitions) through the same `useConfirm()` pattern `handleDelete` already uses. | DSN-8 | Open |
| UX-018 | `/tickets/new`, `/tickets/:id/edit` — `features/tickets/components/TicketFormPage.tsx` | form | major | The customer `SelectField` (lines 166-171) is populated via `getCustomerOptions` with `page_size: 100` and no search/combobox — customers past the 100th (alphabetically) become unselectable with no on-screen indication the list is truncated. | Replace with a searchable combobox querying `/customers/` by typed text, or at minimum show a "showing first 100, search by name" hint. | DSN-10 | Open |
| UX-019 | `/tickets`, `/tickets/my-tickets` — `TicketListPage.tsx`, `MyTicketsPage.tsx` | IA | major | Neither list's `columns` array (`TicketListPage.tsx:76-129`, `MyTicketsPage.tsx:60-101`) includes any SLA/due-date/breach indicator, despite SLA being a first-class per-ticket concept (`TicketSlaSection.tsx`). An agent scanning their queue can't spot at-risk/breached tickets without opening each one. | Add a compact SLA-status column (reusing `TicketSlaSection`'s badge-variant logic) to both list views. | DSN-11 | Open |
| UX-020 | `/tickets/:id`, `/tickets`, `/tickets/:id` conversation — `TicketAssigneeControl.tsx:54`, `TicketListPage.tsx:154`, `TicketConversation.tsx:145` | consistency | minor | These 3 standalone `SelectTrigger`s (explicitly named as deferred by `DSN-2`) carry no `title` attribute for unbounded text (agent names, category names, quick-reply titles) — unlike `SelectField`'s `SelectTrigger`, already fixed for this. | Add `title={displayValue}` to all 3, mirroring `SelectField`'s fix. **Corrected during Story 62 implementation:** `TicketConversation.tsx`'s quick-reply `SelectTrigger` is a verified false positive — `selectedQuickReplyId` resets to `''` immediately after every pick (`TicketConversation.tsx:117-123`), so its trigger only ever displays the placeholder, never a persisted value; nothing to truncate. Only the other 2 sites were fixed. | DSN-7 | Resolved (Story 62) — partial, 2 of 3 sites; 3rd site confirmed a false positive |
| UX-021 | `/tickets/:id` — `TicketDetailPage.tsx:89`, `TicketSlaSection.tsx:40` vs `CustomerContextPanel.tsx:44` | responsive | minor | The first two use a fixed `grid-cols-2` with no `sm:` fallback; `CustomerContextPanel`'s own detail list uses `flex flex-col` (stacks by default) — an inconsistent responsive strategy for the same "label/value pair list" pattern on the same route. | Standardize on one detail-list component/pattern (`grid-cols-1 sm:grid-cols-2`) reused by all three. | DSN-9 | Open |
| UX-022 | `/tickets/my-tickets` — `features/tickets/components/MyTicketsPage.tsx` | consistency | minor | The `DataTable`'s `empty` prop (line 142) always shows the same "no assigned tickets" message regardless of whether the queue is truly empty or a status/priority filter returned nothing — unlike `TicketListPage.tsx` (lines 199-205), which branches on active-filter state. | Branch the empty state on `statusFilter !== 'all' || priorityFilter !== 'all'`, mirroring `TicketListPage`. | DSN-7 | Resolved (Story 62) |
| UX-023 | `/tickets/:id` — `features/tickets/components/InternalNotesSection.tsx` | content | minor | The delete button reads "Remove" (`t('internalNotes.actions.remove')`, line 146) but its confirm-dialog title reads "Delete this note?" (line 114) — breaks the "Remove ⇄ Remove this X?" pairing every other delete-confirm flow in the app uses consistently. | Change `internalNotes.delete.title`/`description` (`en`/`ar`) to "Remove this note?" for consistency. | DSN-12 | Open |
| UX-024 | `/knowledge-base/manage/new` `FaqFormPage.tsx:18-25` vs `/knowledge-base/articles/manage/new` `ArticleFormPage.tsx:28-38` | bilingual | major | Articles have a fully bilingual model (`title_en`/`title_ar`/`body_en`/`body_ar`), but FAQs have only `question`/`answer` with no language split at all — an Arabic-speaking customer sees FAQs only in whatever single language they were authored in. | Add `question_ar`/`answer_ar` to the FAQ model/form, mirroring Articles, or explicitly scope FAQs as English-only. | DSN-12 | Open |
| UX-025 | `/knowledge-base/manage/new\|:id/edit`, `/knowledge-base/articles/manage/new\|:id/edit` — `FaqFormPage.tsx`, `ArticleFormPage.tsx` | interaction | major | Neither form warns before navigating away with unsaved changes — no `beforeunload`/router-blocking anywhere. Riskiest for `ArticleFormPage.tsx`, where one accidental back-navigation loses up to 20,000 chars each of simultaneously-edited English/Arabic content (`schemas.ts` `requiredString(20000)`). | Add a shared "confirm before leaving with unsaved changes" guard reusable across both forms. | DSN-8 | Open |
| UX-026 | `frontend/src/features/knowledge-base/components/MarkdownField.tsx` (used by `ArticleFormPage.tsx`) | form | minor | The "Write" tab (lines 28-30) is a bare `TextareaField` with no visible markdown syntax hint or formatting toolbar — an editor must recall markdown syntax from memory, only checkable via the Preview tab. | Add a small "Markdown supported" helper line with a syntax-reference link below the textarea. | DSN-10 | Open |
| UX-027 | `frontend/src/features/knowledge-base/components/MarkdownPreview.tsx` (used by `ArticleFormPage.tsx`, `ArticleReaderPage.tsx`, `SearchPage.tsx`) | content | major | Imports plain `react-markdown` with no `remark-gfm` plugin (line 1) — GFM syntax (tables, strikethrough, task lists, autolinked URLs) isn't parsed and renders as literal raw text in both the live preview and the published article. | Add `remark-gfm` to `<Markdown remarkPlugins={[remarkGfm]}>`. | DSN-12 | Open |
| UX-028 | `/knowledge-base/manage`, `/knowledge-base/articles/manage` — `FaqListPage.tsx:51-59`, `ArticleListPage.tsx:70-78` | interaction | minor | Both tables support only single-row delete — no checkbox column/bulk action bar, so removing many FAQs/articles requires one confirm round-trip per row. (Still-open per `DSN-2`'s deferred list.) | Add a shared bulk-selection column + action bar to `DataTable`, wired into both pages together. | DSN-8 | Open |
| UX-029 | `/knowledge-base/manage`, `/knowledge-base/articles/manage` — `FaqListPage.tsx`, `ArticleListPage.tsx` | IA | minor | Neither manage table has a text search or filter input — `useServerTable` only exposes sort/pagination; `ArticleListPage.tsx` doesn't even filter by the `status`/`category_name` columns it displays. | Add a search box (and status/category filter for articles) to both manage list pages. | DSN-11 | Open |
| UX-030 | `/knowledge-base/manage/new` `FaqFormPage.tsx:18-25` vs `/knowledge-base/articles/manage/new` `ArticleFormPage.tsx:37` | consistency | major | Articles have an explicit draft/publish workflow (`status` field + `SelectField`); FAQs have no status field at all, and `FaqBrowsePage.tsx:20` fetches every FAQ unfiltered — every FAQ save goes live immediately with no way to save a work-in-progress FAQ, unlike Articles. | Add a `status` field to the FAQ model/form matching the Article draft/published pattern, or explicitly document FAQs as always-live. | DSN-7 | Deferred — requires a backend FAQ status model field + migration; outside `DSN-6`–`DSN-13`'s frontend-only guardrail (`SupportOs backlog.MD:556`). Needs a dedicated non-`DSN` story or an explicit guardrail exception. |
| UX-031 | `/knowledge-base` `FaqBrowsePage.tsx:54-56` vs `/knowledge-base/search` `SearchPage.tsx:60-62` | content | major | FAQ answers are authored/rendered as plain text (`whitespace-pre-wrap`) on the browse page, but the same field is piped through the Markdown renderer on the search-results page — any `*`/`_`/`#`/backtick typed as literal punctuation renders differently depending on which screen it's viewed from. | Only pass FAQ text through `MarkdownPreview` if FAQs are meant to support markdown; otherwise render plain text in `SearchPage.tsx` too, matching `FaqBrowsePage`. | DSN-12 | Open |
| UX-032 | `/knowledge-base/articles/manage` — `features/knowledge-base/components/ArticleListPage.tsx:42-47` | bilingual | minor | The manage table's title column always shows `row.title_en` regardless of admin UI language, unlike `ArticleBrowsePage.tsx:49-51`/`ArticleReaderPage.tsx:38-39`, which both switch on `isArabic`. An Arabic-UI editor can't identify rows by their Arabic title. | Show the title in the admin's current UI language, consistent with browse/reader. | DSN-12 | Open |
| UX-033 | `/knowledge-base/articles` `ArticleBrowsePage.tsx:24-32` vs `/knowledge-base` `FaqBrowsePage.tsx:24-40` | consistency | major | `FaqBrowsePage` cross-links to Articles, Search, and a `Can`-gated Manage link; `ArticleBrowsePage` only links to Search — no link back to FAQs and no manage-link even for `knowledge_base.manage` holders. | Add the same `Can permission="knowledge_base.manage"` manage-link and a link to `/knowledge-base` in `ArticleBrowsePage`'s header. | DSN-7 | Resolved (Story 62) |
| UX-034 | `/knowledge-base/articles`, `/knowledge-base/articles/:id` — `ArticleBrowsePage.tsx`, `ArticleReaderPage.tsx` | IA | critical | Both screens are gated only on `knowledge_base.view`, yet both conditionally render a "draft" badge for non-published articles (`ArticleBrowsePage.tsx:57-59`, `ArticleReaderPage.tsx:40-42`) with no status filter on either query (`:21`, `:29`) — implying unpublished drafts (full title+body) may be reachable by any view-only user, with the badge only signaling draft state after the fact rather than preventing exposure. | Confirm with backend whether the API scopes non-manage callers to `status=published`; if not, filter drafts out client-side for `knowledge_base.view`-only users. Flag for verification alongside implementation — may need a backend check beyond this thread's frontend-only scope. | DSN-11 | Open |
| UX-035 | `/knowledge-base/search` — `features/knowledge-base/components/SearchPage.tsx:61,80-82` | content | major | Search-result headlines render through `MarkdownPreview`, which deliberately doesn't render embedded HTML (`MarkdownPreview.tsx:4-6`) — a typical search backend's `<b>`/`<mark>` highlight tags around matched terms would be escaped and shown as literal text instead of highlighted. | Render `headline`/`headline_en`/`headline_ar` as sanitized inline HTML (or a dedicated highlight component), reserving `MarkdownPreview` for full body content. | DSN-12 | Open |
| UX-036 | `/knowledge-base/search` — `features/knowledge-base/components/SearchPage.tsx:61` vs `:80-82` | consistency | minor | The FAQ result branch falls back to the full answer when no headline exists; the article branch has no equivalent fallback (`... \|\| ''`) — an article result with an empty headline renders a blank card body. | Fall back to a truncated `body_en`/`body_ar` for article results, mirroring the FAQ branch. **Corrected during Story 62 implementation:** `ArticleSearchResult` (`frontend/src/features/knowledge-base/types/searchResult.ts:12-21`) has no `body_en`/`body_ar` fields — the search API never returns them. Added a translated placeholder (`search.noPreview`) instead, since adding those fields would be an API response-shape change, forbidden by the guardrail. | DSN-7 | Resolved (Story 62) — placeholder fallback, not body text |
| UX-037 | `/knowledge-base/articles` — `features/knowledge-base/components/ArticleBrowsePage.tsx` | IA | minor | Category is shown as a per-card badge (lines 54-56) and selectable in the authoring form, but the browse page has no filter/group-by-category control — with up to 100 articles fetched flat, users must scan the whole list to find a category's articles. | Add a category filter control above the list, reusing the same `useCategories` options `ArticleFormPage.tsx` already fetches. | DSN-11 | Open |
| UX-038 | `/roles`, `/categories`, `/tasks` vs `/users` — `RoleListPage.tsx`, `CategoryListPage.tsx`, `TaskListPage.tsx` vs `UserListPage.tsx:33-45,102-107` | consistency | major | `UserListPage` has a debounced text-search `Input`; the other three "simplest, most similar" list screens have no equivalent — `RoleListPage`/`CategoryListPage` offer no filtering beyond column sort, `TaskListPage` only offers a fixed completed-status `Select`, no text search by title. | Extract `UserListPage`'s debounced-search pattern into a shared hook/component and apply it to Roles, Categories, and Tasks. **Corrected during Story 62 implementation:** `RoleViewSet`/`CategoryViewSet` (`backend/apps/accounts/views.py:167`, `backend/apps/tickets/views.py:44`) already have `search_fields` server-side, so Role/Category got the shared `useDebouncedSearch` hook + search box. `TaskViewSet` (`backend/apps/agents/views.py:18-34`) has no `search_fields` at all — wiring search there would silently no-op; deferred, needs a backend one-liner outside this story's frontend-only scope. | DSN-7 | Resolved (Story 62) — partial, Role + Category only; Task deferred |
| UX-039 | `/roles`, `/categories` — `RoleListPage.tsx:29-37`, `CategoryListPage.tsx:29-37` | interaction | major | `handleDelete`'s `confirm()` dialog shows only static text — no usage count (e.g. "N users have this role" / "N tickets use this category") is fetched or shown, so a heavily-referenced role/category gets the same generic warning as an unused one. | Fetch/display a usage count in the confirm dialog (or block deletion server-side and surface the count in the resulting error) before commit. | DSN-8 | Open |
| UX-040 | `/roles`, `/categories`, `/tasks` — `RoleListPage.tsx:36`, `CategoryListPage.tsx:36`, `TaskListPage.tsx:64` | interaction | major | None of the three delete flows handle mutation failure — each calls `mutateAsync` with no `onError`/`catch`, invoked as `void handleDelete(row)` (discarding the promise). A rejected delete (e.g. an FK constraint) shows the admin nothing; the row just silently stays. | Add an `onError` handler to each delete mutation that surfaces a toast, matching the form-submit error pattern already used elsewhere. | DSN-8 | Open |
| UX-041 | `/roles/new`, `/roles/:id/edit` — `features/accounts/components/RoleFormPage.tsx:154,181,186` | IA | minor | The page `<h1>` (line 154) is followed directly by a permission-group `<h3>` (line 186) — the "Permissions" text at line 181 is a `FormLabel` (`<label>`), not a heading — skipping `<h2>` and breaking the logical heading hierarchy. | Change the group heading to `<h2>`, or add an explicit `<h2>` "Permissions" heading above it. | DSN-11 | Open |
| UX-042 | `/roles/new`, `/roles/:id/edit` — `features/accounts/components/RoleFormPage.tsx:176-208` | content | major | The permission checklist shows each entry only as a raw dotted slug in monospace (line 199, e.g. `tickets.manage`), grouped by a computed area-prefix heading — no human-readable description of what any permission actually grants anywhere on the page. | Add a short descriptive string (tooltip or inline secondary text) per permission, from a small static lookup keyed by slug — the slug itself stays untranslated per `CONVENTIONS.md` §23. | DSN-12 | Open |
| UX-043 | `/roles/new`, `/roles/:id/edit` — `features/accounts/components/RoleFormPage.tsx:186-198` | interaction | minor | No "select all in group" affordance — only individual per-permission checkboxes. Granting an entire functional area (e.g. all `tickets.*`) requires clicking every checkbox in that group one at a time. | Add a "select all in group" checkbox/link next to each area heading that toggles the whole group's array. | DSN-8 | Open |
| UX-044 | `/tasks/new`, `/tasks/:id/edit` vs `/users/new`, `/roles/new`, `/categories/new` — `TaskFormPage.tsx:140` | consistency | minor | `TaskFormPage` wraps its form in `max-w-lg`; `UserFormPage`, `RoleFormPage`, `CategoryFormPage` all use `max-w-2xl`. Task's field set is comparable in size, with no apparent reason for the narrower width. | Standardize all four single-record form pages on `max-w-2xl`. **Corrected during Story 62 implementation:** a fresh `grep -rn "max-w-2xl\|max-w-lg" frontend/src/features` across all 10 form pages shows `max-w-lg` is actually the 6-file majority (`CustomerFormPage`, `PortalTicketFormPage`, `PortalFeedbackFormPage`, `FaqFormPage`, `TicketFormPage`, `TaskFormPage`) — `TaskFormPage` already matched it, no change needed there. `RoleFormPage`'s width is justified by its permission-checklist grid; `ArticleFormPage`'s by its two side-by-side English/Arabic content `Card`s — both kept `max-w-2xl`. `CategoryFormPage` and `UserFormPage` had no such content justifying the extra width, so both were narrowed to `max-w-lg` instead, converging on the actual majority. | DSN-7 | Resolved (Story 62) — corrected: `CategoryFormPage`/`UserFormPage` narrowed to `max-w-lg`, not `TaskFormPage` widened |
| UX-045 | `/tasks` — `features/tasks/components/TaskListPage.tsx:141-143` | bilingual | minor | The standalone status-filter `SelectTrigger` (one of the `DSN-2`-deferred standalone usages) has no `title` attribute and a fixed `w-40` — a longer Arabic translation of the selected filter option could truncate with no way to see the full text. | Add a `title` mirroring the selected value's label, or relax to `min-w-40 w-auto`. | DSN-12 | Open |
| UX-046 | `/users/new\|:id/edit`, `/roles/new\|:id/edit`, `/categories/new\|:id/edit`, `/tasks/new\|:id/edit` — `UserFormPage.tsx`, `RoleFormPage.tsx`, `CategoryFormPage.tsx`, `TaskFormPage.tsx` | interaction | minor | All four forms render only a submit button, no Cancel/Back control — the only way to abandon an in-progress edit is the browser back button or the nav sidebar. | Add a secondary "Cancel" button next to Save on all four, navigating back to the corresponding list route. | DSN-8 | Open |
| UX-047 | `/reports/agents` — `features/reports/components/AgentReportsPage.tsx:93-115` | consistency | major | `CONVENTIONS.md`'s own Chart-type guidance (line ~1633) records "Horizontal Bar Chart... ≤15 agents before switching to a paginated table" for this report, but the page renders `<BarChart>` unconditionally with no length check or table fallback — an org with 15+ agents gets an uncapped, increasingly cramped chart instead of the documented table. | Add a row-count guard that switches to `DataTable` (already used elsewhere, e.g. `AuditLogListPage.tsx`) when `rows.length > 15`. **Corrected during Story 62 implementation — verified false positive:** `backend/apps/reports/agents.py:30` defines `MAX_AGENTS = 15`, and `agent_performance()` (line 121) already does `sorted(values.items(), key=..., reverse=True)[:MAX_AGENTS]` — the backend already caps every response at exactly 15 agents, sorted descending by the selected metric. `rows.length` can never exceed 15; the finding's premise doesn't hold. No frontend change made. | DSN-7 | Resolved (Story 62) — verified false positive, no code change needed |
| UX-048 | `/reports/tickets` — `features/reports/components/TicketReportsPage.tsx:36-55` | content | major | `toChartSeries` silently truncates the volume chart to the first 5 series (`MAX_SERIES = 5`, `.slice(0, MAX_SERIES)`) whenever a "series by" dimension has more than 5 values, with no badge/note/indicator anywhere that data is being hidden. | Render a visible "Showing top 5 of N" note next to the chart title whenever truncation occurs. | DSN-12 | Open |
| UX-049 | `/reports/tickets`, `/reports/sla`, `/reports/agents`, `/reports/csat`, `/reports/dashboard` — shared `frontend/src/shared/ui/chart/ChartFrame.tsx:58,61-97` | interaction | major | The "Export CSV" `action` button is rendered unconditionally in `CardHeader`, outside the `query.isPending`/`isError`/`isEmpty`/`isSuccess` branches — the export button stays clickable while data is loading, after the query errors, and when the chart is empty, producing a blank/stale CSV with no warning, on all 5 report pages at once. | Disable/hide the export action in `ChartFrame` itself when `query.isPending`, `query.isError`, or `isEmpty?.(query.data)` is true. | DSN-8 | Open |
| UX-050 | `/reports/dashboard` — `features/reports/components/ManagementDashboardPage.tsx` | IA | major | The file contains no `Link`/`useNavigate` to `/reports/tickets`, `/reports/sla`, `/reports/agents`, or `/reports/csat` — the 4 KPI gauges (lines 92-104) each map 1:1 to one of those reports but are a dead end; a manager spotting an outlier can't drill in. | Make each gauge's label (or a small "View report" link) navigate to its corresponding report route, carrying over the current `from`/`to` filter. | DSN-11 | Open |
| UX-051 | All 5 report pages — `TicketReportsPage.tsx:116-132`, `SlaReportsPage.tsx:93-109`, `AgentReportsPage.tsx:61-77`, `CsatReportsPage.tsx:89-105`, `ManagementDashboardPage.tsx:48-64` | form | minor | Every "to" date `<Input type="date">` has no `min` tied to the "from" value and no inline validation — a user can pick an end date before the start date; the query just fires with an inverted range and surfaces only as an opaque empty/error state. | Add `min={from \|\| undefined}` to each "to" input, or extract a shared `DateRangeFilter` used by all 5 (the filter markup is currently duplicated verbatim). | DSN-10 | Open |
| UX-052 | All 5 report pages (same filter bars as UX-051) | interaction | minor | Raw `type="date"` inputs with no quick presets (Last 7/30 days, This month) — a manager must manually compute and type both boundary dates on every one of the 5 pages, every time. | Add a shared preset dropdown/button group next to the date inputs, wired through the existing `from`/`to` state. | DSN-8 | Open |
| UX-053 | `/settings` — `features/organization/components/SettingsPage.tsx:86-88` | interaction | major | `StringListField`'s remove control is a bare `<button type="button" onClick={...}>` with an `XIcon` and no `aria-label`, not using the shared `Button` primitive — a genuinely new icon-only button that postdates `DSN-2`'s "all 5 icon-only buttons already have labels" audit. | Give it an `aria-label` and swap to the shared `Button` `variant="ghost" size="icon-xs"`, matching the rest of the codebase's icon-button convention. | DSN-8 | Open |
| UX-054 | `*` (404) — `frontend/src/app/NotFoundPage.tsx:6-11` | consistency | major | Renders a bare `<div><p>...</p><Link>...</Link></div>` with no heading element, no icon/Card, and an unstyled text `Link` — every sibling public screen (`LoginPage`, `LiveChatWidget`, `WebFormPage`) uses a consistent icon-in-circle + `<h1>` + `Card` pattern this page skips entirely, including having no page-level heading at all. | Give it the same icon/`<h1>`/`Card` treatment as the other public screens; render the "go home" link as a `Button`. | DSN-7 | Resolved (Story 62) |
| UX-055 | All routes (crash fallback) — `frontend/src/app/RouteErrorBoundary.tsx:12-30` | interaction | critical | None of the 3 branches offer recovery navigation — `<ErrorState error={error} />` (line 13) has no `onRetry` so its retry button never renders; the other two branches are bare `role="alert"` divs with no link/button. Since `errorElement` sits on the top-level route objects (`router.tsx:17,45,440`), a crash anywhere replaces the entire layout — including `RootLayout`'s `Sidebar` — leaving the user with no nav, no home link, nothing. | Add a "Go home" `Button`/`Link` common to all 3 branches; pass `onRetry` to the `ErrorState` branch. | DSN-8 | Open |
| UX-056 | All routes (crash fallback) — `frontend/src/app/RouteErrorBoundary.tsx:20-22` | bilingual | minor | `{error.status} {error.statusText}` renders the raw HTTP reason phrase (e.g. "Not Found") never routed through `t(...)` — always displays in English even in the Arabic UI, unlike the generic fallback (line 28), which is properly translated. | Drop `statusText` or map `error.status` to a translated message. | DSN-12 | Open |
| UX-057 | `/portal/tickets/:id` — `features/portal/components/PortalTicketDetailPage.tsx` | IA | critical | The customer/agent message thread (`TicketConversation`, which renders both inbound and outbound messages, not internal notes) is entirely omitted from the portal detail view — justified in a comment as "staff/agent-only," but the component itself only gates the reply *form* to staff, not reading. A customer has no way to see any agent response until the ticket is resolved/closed and they're asked for feedback. | Add a read-only, portal-scoped conversation view (reusing `Message`'s read path, not the staff `ReplyForm`) to `PortalTicketDetailPage`. | DSN-11 | Open |
| UX-058 | `/portal/tickets/:id/feedback` — `features/portal/components/PortalFeedbackFormPage.tsx:68-95` | IA | major | No back/cancel link at all, unlike every other deep portal page (`PortalArticleReaderPage.tsx:36`, `PortalTicketDetailPage.tsx:31` both provide one). Since the flat portal nav doesn't cover this route, the only way out without submitting is the browser back button. | Add a `Link` back to `/portal/tickets/${ticketId}`, matching the other two deep pages. | DSN-11 | Open |
| UX-059 | `/portal/tickets/:id` — `features/portal/components/PortalTicketDetailPage.tsx:45-90` | IA | minor | The status/priority/category/agent/created_at `<dl>` (lines 45-84) closes before a second Description `dt`/`dd` pair (lines 85-90) that sits as a sibling `<div>`, not nested inside the `<dl>` — `dt`/`dd` are only valid as `dl` children, breaking the term/definition relationship for assistive tech. | Move the Description `dt`/`dd` inside the same `<dl>` (or wrap both blocks in one `<dl>`). | DSN-11 | Open |
| UX-060 | `/portal/tickets/:id/feedback` — `features/portal/components/PortalFeedbackFormPage.tsx:46` | form | minor | `defaultValues: { rating: 'satisfied', comment: '' }` pre-selects the most positive CSAT rating — a customer who submits without deliberately choosing a rating records a positive score by default, skewing CSAT data reported elsewhere (`RPT-4`). | Ship the rating field with no pre-selected value, required-but-unset by default, so submission requires an explicit choice. | DSN-10 | Open |
| UX-061 | `/portal/tickets` — `features/portal/components/PortalTicketListPage.tsx:116` | consistency | minor | The status-filter `SelectTrigger` has `aria-label` but no `title` attribute — one of the 6 `DSN-2`-deferred standalone `SelectTrigger` usages, now confirmed present in the portal too. | Add `title` mirroring the selected option's label, consistent with `SelectField`'s fix. | DSN-7 | Resolved (Story 62) |
| UX-062 | `/portal/tickets/new`, `/portal/tickets/:id/feedback` — `PortalTicketFormPage.tsx:66-68`, `PortalFeedbackFormPage.tsx:88-90` | interaction | minor | Both submit buttons get only `disabled={mutation.isPending}` — no "Submitting…" label or spinner, just a dimmed button. More consequential here than on staff forms since customers are more likely on a slower mobile connection. | Add a loading label/spinner state to the shared `Button` (or a small wrapper) for use on these two customer-write screens. | DSN-8 | Open |
| UX-063 | `/portal` — `features/portal/components/PortalHomePage.tsx:13-23` | IA | minor | The quick-link row surfaces only 3 of the portal's 6 destinations (New ticket, My tickets, FAQs) — omitting Articles and Ticket History, both equally prominent top-nav items. A first-time customer has no home-page path into the article library or ticket-history archive. | Add "Articles" and "History" links to the home page's quick-link row. | DSN-11 | Open |
| UX-064 | `/portal/articles/:id` — `features/portal/components/PortalMarkdownPreview.tsx:10-16` | responsive | minor | `prose prose-sm` has no width/overflow override and `index.css` has no `.prose` customization — Tailwind Typography's `prose` doesn't wrap `table` elements in a scroll container, so an article with a markdown table overflows a 375px viewport instead of scrolling within its own container. | Wrap markdown table output in an `overflow-x-auto` container (a `table` component override for `react-markdown`, or a scoped `.prose table` rule). | DSN-9 | Open |
| UX-065 | `/portal/tickets/:id` — `features/portal/components/PortalTicketDetailPage.tsx:45` | responsive | minor | The field `<dl className="grid grid-cols-2 gap-4">` is hardcoded to 2 columns with no smaller-viewport breakpoint — at 375px each column is ~150px, cramping longer values (agent names, category names) into a narrow half-width cell. The staff `TicketDetailPage.tsx:89` shares the identical pattern, but the portal is the surface most likely used on a phone. | Add `grid-cols-1 sm:grid-cols-2` to this `dl` so fields stack on narrow viewports. | DSN-9 | Open |
| UX-066 | `/reports/sla`, `/reports/agents`, `/reports/csat` — `SlaReportsPage.tsx:87`, `AgentReportsPage.tsx:55`, `CsatReportsPage.tsx:83` | consistency | major | Discovered during Story 62 (`DSN-7`) implementation, not by `DSN-6`'s original walk: all three pages call `PageHeader title={t('title')}`, which resolves the `reports` namespace's bare `title` key — "Ticket Reports" — on every one of them. `SlaReportsPage`, `AgentReportsPage`, and `CsatReportsPage` all showed the wrong page title in production, identical to `TicketReportsPage`'s. | Point each page's `PageHeader` at its own existing key instead: `sidebarSla`/`sidebarAgents`/`sidebarCsat` (the same strings already used for their sidebar nav links, per `dashboard.title`'s precedent for `ManagementDashboardPage`, which was already correct). No new translation strings needed. | DSN-7 | Resolved (Story 62) |
