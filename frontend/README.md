# SupportOS frontend

Run steps live in the root [`README.md`](../README.md). File-placement rules
live in [`src/README.md`](src/README.md). API conventions (the response
envelope, error handling, retries, query keys) live in the root README's
**API conventions** section.
